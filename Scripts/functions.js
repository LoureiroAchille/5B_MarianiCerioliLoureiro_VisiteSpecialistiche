const hide = (div) => {
    let hidden = '<class="hide">';
    hidden += div;
    hidden += '</class>'
    innerHTML = hidden; 
}

const show = (div) => {
    let hidden = '<class="show">';
    hidden += div;
    hidden += '</class>'
}

